import { ContentTypeHtmlDirective } from './content-type-html.directive';

describe('ContentTypeHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentTypeHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
