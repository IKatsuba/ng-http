import { SomeControllerDirective } from './some-controller.directive';

describe('GetControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeControllerDirective();
    expect(directive).toBeTruthy();
  });
});
